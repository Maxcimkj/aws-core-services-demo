spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: note-manager
  datasource:
    url: jdbc:postgresql://note-manager-database.cjm4c0ckadut.eu-north-1.rds.amazonaws.com:5432/postgres?sslmode=require
    username: postgres
    password: 1PS3fZs5inUsOEznXEz7
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://cognito-idp.eu-north-1.amazonaws.com/eu-north-1_Oe4Ubp5pI

aws:
  region: eu-north-1

server:
  port: 8080

logging:
  level:
    software.amazon.awssdk: WARN
    com.example.note_manager: INFO
    root: INFO
